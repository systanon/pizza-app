services:
  frontend:
    build:
      context: .
      args:
        APP_PORT: ${APP_PORT:-3000}
    restart: unless-stopped
    ports:
      - '${APP_PORT:-3000}:${APP_PORT:-3000}'
    env_file: .env
    networks:
      - app-network

networks:
  app-network:
    external: true
